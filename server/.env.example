# Copy to .env and fill in for local dev.
# On Render/Railway/Heroku, set these as dashboard env vars instead.

PORT=4000

# The exact URL your Vercel client is deployed at, e.g. https://cricket-auction.vercel.app
# Use * only for local testing.
CLIENT_ORIGIN=*
